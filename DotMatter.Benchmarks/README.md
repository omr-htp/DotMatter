# DotMatter.Benchmarks

Benchmark harness for `DotMatter.Core` hot paths.

Current coverage:

- TLV encode/decode
- CASE transcript key derivation

Run locally with:

```powershell
dotnet run --project DotMatter.Benchmarks -c Release
```
