// <auto-generated/>
// Generated by DotMatter.CodeGen from CHIP SDK v1.5 (connectedhomeip).
// XML: commodity-price-cluster.xml
// Cluster: Commodity Price (0x0095)
// To regenerate: dotnet run --project DotMatter.CodeGen

#nullable enable

using DotMatter.Core.InteractionModel;
using DotMatter.Core.Sessions;
using DotMatter.Core.TLV;
using System.Text.Json.Nodes;

namespace DotMatter.Core.Clusters;

/// <summary>Commodity Price (0x0095, rev 4)</summary>
public class CommodityPriceCluster : ClusterBase
{
    /// <summary>Matter cluster identifier.</summary>
    public const uint ClusterId = 0x0095;

    /// <inheritdoc />
    protected override uint GetClusterId() => ClusterId;

    /// <summary>Creates a new <see cref="CommodityPriceCluster"/> bound to the given session and endpoint.</summary>
    public CommodityPriceCluster(ISession session, ushort endpointId = 1)
        : base(session, endpointId) { }

    /// <summary>Feature flags for this cluster.</summary>
    [System.Flags]
    public enum Feature : uint
    {
        /// <summary>Forecasts upcoming pricing</summary>
        Forecasting = 1u << 0,
    }

    /// <summary>TariffUnitEnum enum values.</summary>
    public enum TariffUnitEnum : byte
    {
        /// <summary>kWh value.</summary>
        KWh = 0x00,
        /// <summary>kVAh value.</summary>
        KVAh = 0x01,
    }

    /// <summary>TariffPriceTypeEnum enum values.</summary>
    public enum TariffPriceTypeEnum : byte
    {
        /// <summary>Standard value.</summary>
        Standard = 0x00,
        /// <summary>Critical value.</summary>
        Critical = 0x01,
        /// <summary>Virtual value.</summary>
        Virtual = 0x02,
        /// <summary>Incentive value.</summary>
        Incentive = 0x03,
        /// <summary>IncentiveSignal value.</summary>
        IncentiveSignal = 0x04,
    }

    /// <summary>CommodityPriceDetailBitmap bitmap flags.</summary>
    [System.Flags]
    public enum CommodityPriceDetailBitmap : uint
    {
        /// <summary>Description flag.</summary>
        Description = 1u << 0,
        /// <summary>Components flag.</summary>
        Components = 1u << 1,
    }

    /// <summary>CommodityPriceComponentStruct struct.</summary>
    public sealed class CommodityPriceComponentStruct
    {
        /// <summary>Gets or sets Price.</summary>
        public long Price { get; set; }
        /// <summary>Gets or sets Source.</summary>
        public TariffPriceTypeEnum Source { get; set; } = default!;
        /// <summary>Gets or sets Description.</summary>
        public string? Description { get; set; }
        /// <summary>Gets or sets TariffComponentID.</summary>
        public uint? TariffComponentID { get; set; }
    }

    /// <summary>CommodityPriceStruct struct.</summary>
    public sealed class CommodityPriceStruct
    {
        /// <summary>Gets or sets PeriodStart.</summary>
        public uint PeriodStart { get; set; }
        /// <summary>Gets or sets PeriodEnd.</summary>
        public uint? PeriodEnd { get; set; }
        /// <summary>Gets or sets Price.</summary>
        public long? Price { get; set; }
        /// <summary>Gets or sets PriceLevel.</summary>
        public short? PriceLevel { get; set; }
        /// <summary>Gets or sets Description.</summary>
        public string? Description { get; set; }
        /// <summary>Gets or sets Components.</summary>
        public CommodityPriceComponentStruct[]? Components { get; set; }
    }

    /// <summary>CurrencyStruct struct.</summary>
    public sealed class CurrencyStruct
    {
        /// <summary>Gets or sets Currency.</summary>
        public ushort Currency { get; set; }
        /// <summary>Gets or sets DecimalPoints.</summary>
        public byte DecimalPoints { get; set; }
    }

    // TLV struct serializers

    private static void WriteCommodityPriceComponentStruct(MatterTLV tlv, byte tag, CommodityPriceComponentStruct value)
    {
        ArgumentNullException.ThrowIfNull(value);
        tlv.AddStructure(tag);
        WriteCommodityPriceComponentStructFields(tlv, value);
        tlv.EndContainer();
    }

    private static void WriteCommodityPriceComponentStruct(MatterTLV tlv, CommodityPriceComponentStruct value)
    {
        ArgumentNullException.ThrowIfNull(value);
        tlv.AddStructure();
        WriteCommodityPriceComponentStructFields(tlv, value);
        tlv.EndContainer();
    }

    private static void WriteCommodityPriceComponentStructFields(MatterTLV tlv, CommodityPriceComponentStruct value)
    {
        tlv.AddInt64(0, value.Price);
        tlv.AddUInt8(1, (byte)value.Source);
        if (value.Description != null) tlv.AddUTF8String(2, value.Description);
        if (value.TariffComponentID != null) tlv.AddUInt32(3, value.TariffComponentID.Value);
    }

    private static void WriteCommodityPriceStruct(MatterTLV tlv, byte tag, CommodityPriceStruct value)
    {
        ArgumentNullException.ThrowIfNull(value);
        tlv.AddStructure(tag);
        WriteCommodityPriceStructFields(tlv, value);
        tlv.EndContainer();
    }

    private static void WriteCommodityPriceStruct(MatterTLV tlv, CommodityPriceStruct value)
    {
        ArgumentNullException.ThrowIfNull(value);
        tlv.AddStructure();
        WriteCommodityPriceStructFields(tlv, value);
        tlv.EndContainer();
    }

    private static void WriteCommodityPriceStructFields(MatterTLV tlv, CommodityPriceStruct value)
    {
        tlv.AddUInt32(0, value.PeriodStart);
        if (value.PeriodEnd != null) { tlv.AddUInt32(1, value.PeriodEnd.Value); } else { tlv.AddNull(1); }
        if (value.Price != null) tlv.AddInt64(2, value.Price.Value);
        if (value.PriceLevel != null) tlv.AddInt16(3, value.PriceLevel.Value);
        if (value.Description != null) tlv.AddUTF8String(4, value.Description);
        if (value.Components != null) { tlv.AddArray(5); foreach (var item in value.Components) { WriteCommodityPriceComponentStruct(tlv, item); } tlv.EndContainer(); }
    }

    private static void WriteCurrencyStruct(MatterTLV tlv, byte tag, CurrencyStruct value)
    {
        ArgumentNullException.ThrowIfNull(value);
        tlv.AddStructure(tag);
        WriteCurrencyStructFields(tlv, value);
        tlv.EndContainer();
    }

    private static void WriteCurrencyStruct(MatterTLV tlv, CurrencyStruct value)
    {
        ArgumentNullException.ThrowIfNull(value);
        tlv.AddStructure();
        WriteCurrencyStructFields(tlv, value);
        tlv.EndContainer();
    }

    private static void WriteCurrencyStructFields(MatterTLV tlv, CurrencyStruct value)
    {
        tlv.AddUInt16(0, value.Currency);
        tlv.AddUInt8(1, value.DecimalPoints);
    }

    // TLV struct deserializers

    private static CommodityPriceComponentStruct ReadCommodityPriceComponentStruct(MatterTLV tlv)
    {
        var value = new CommodityPriceComponentStruct();
        tlv.OpenStructure();
        while (!tlv.IsEndContainerNext())
        {
            switch (tlv.PeekTag())
            {
                case 0:
                    value.Price = tlv.GetSignedInt(0);
                    break;
                case 1:
                    value.Source = (TariffPriceTypeEnum)tlv.GetUnsignedIntAny(1);
                    break;
                case 2:
                    if (tlv.IsNextNull()) { tlv.GetNull(2); } else { value.Description = tlv.GetUTF8String(2); }
                    break;
                case 3:
                    if (tlv.IsNextNull()) { tlv.GetNull(3); } else { value.TariffComponentID = tlv.GetUnsignedIntAny(3); }
                    break;
                default:
                    tlv.SkipElement();
                    break;
            }
        }

        tlv.CloseContainer();
        return value;
    }

    private static CommodityPriceStruct ReadCommodityPriceStruct(MatterTLV tlv)
    {
        var value = new CommodityPriceStruct();
        tlv.OpenStructure();
        while (!tlv.IsEndContainerNext())
        {
            switch (tlv.PeekTag())
            {
                case 0:
                    value.PeriodStart = tlv.GetUnsignedIntAny(0);
                    break;
                case 1:
                    if (tlv.IsNextNull()) { tlv.GetNull(1); } else { value.PeriodEnd = tlv.GetUnsignedIntAny(1); }
                    break;
                case 2:
                    if (tlv.IsNextNull()) { tlv.GetNull(2); } else { value.Price = tlv.GetSignedInt(2); }
                    break;
                case 3:
                    if (tlv.IsNextNull()) { tlv.GetNull(3); } else { value.PriceLevel = (short)tlv.GetSignedInt(3); }
                    break;
                case 4:
                    if (tlv.IsNextNull()) { tlv.GetNull(4); } else { value.Description = tlv.GetUTF8String(4); }
                    break;
                case 5:
                    if (tlv.IsNextNull()) { tlv.GetNull(5); value.Components = null; break; }
                    var items5 = new List<CommodityPriceComponentStruct>();
                    tlv.OpenArray(5);
                    while (!tlv.IsEndContainerNext())
                    {
                        items5.Add(ReadCommodityPriceComponentStruct(tlv));
                    }
                    tlv.CloseContainer();
                    value.Components = [.. items5];
                    break;
                default:
                    tlv.SkipElement();
                    break;
            }
        }

        tlv.CloseContainer();
        return value;
    }

    private static CurrencyStruct ReadCurrencyStruct(MatterTLV tlv)
    {
        var value = new CurrencyStruct();
        tlv.OpenStructure();
        while (!tlv.IsEndContainerNext())
        {
            switch (tlv.PeekTag())
            {
                case 0:
                    value.Currency = (ushort)tlv.GetUnsignedIntAny(0);
                    break;
                case 1:
                    value.DecimalPoints = (byte)tlv.GetUnsignedIntAny(1);
                    break;
                default:
                    tlv.SkipElement();
                    break;
            }
        }

        tlv.CloseContainer();
        return value;
    }

    /// <summary>Attribute identifiers.</summary>
    public static class Attributes
    {
        /// <summary>TariffUnit (0x0000).</summary>
        public const uint TariffUnit = 0x0000;
        /// <summary>Currency (0x0001).</summary>
        public const uint Currency = 0x0001;
        /// <summary>CurrentPrice (0x0002).</summary>
        public const uint CurrentPrice = 0x0002;
        /// <summary>PriceForecast (0x0003).</summary>
        public const uint PriceForecast = 0x0003;
    }

    /// <summary>Command identifiers.</summary>
    public static class Commands
    {
        /// <summary>GetDetailedPriceRequest (0x0000).</summary>
        public const uint GetDetailedPriceRequest = 0x0000;
        /// <summary>GetDetailedForecastRequest (0x0002).</summary>
        public const uint GetDetailedForecastRequest = 0x0002;
    }

    /// <summary>Event identifiers.</summary>
    public static class Events
    {
        /// <summary>PriceChange (0x0000).</summary>
        public const uint PriceChange = 0x0000;
    }

    /// <summary>Base type for this cluster's event reports.</summary>
    public abstract class ClusterEvent
        : MatterClusterEvent
    {
        /// <summary>Initializes a new cluster event wrapper.</summary>
        protected ClusterEvent(MatterEventReport report, string eventName)
            : base(report, "Commodity Price", eventName) { }
    }

    /// <summary>Fallback event wrapper when DotMatter cannot parse a typed payload.</summary>
    public sealed class UnknownClusterEvent(MatterEventReport report, string? reason = null)
        : ClusterEvent(report, "Unknown")
    {
        /// <summary>Gets the reason the typed payload parser could not materialize this event.</summary>
        public override string? Reason { get; } = reason;
    }

    /// <summary>PriceChange event payload.</summary>
    public sealed class PriceChangeEventData
    {
        /// <summary>Gets or sets CurrentPrice.</summary>
        public CommodityPriceStruct CurrentPrice { get; set; } = default!;
    }

    /// <summary>PriceChange event report.</summary>
    public sealed class PriceChangeEvent(MatterEventReport report, PriceChangeEventData payload)
        : ClusterEvent(report, "PriceChange")
    {
        /// <summary>Gets the typed PriceChange payload.</summary>
        public PriceChangeEventData Payload { get; } = payload;

        /// <inheritdoc />
        public override object? TypedPayload => Payload;
    }

    // Async command methods

    /// <summary>Send GetDetailedPriceRequest command (0x0000).</summary>
    public Task<InvokeResponse> GetDetailedPriceRequestAsync(
        CommodityPriceDetailBitmap details,
        CancellationToken ct = default)
        => InvokeCommandAsync(0x0000, tlv => { tlv.AddUInt16(0, (ushort)details); }, ct);

    /// <summary>Send GetDetailedForecastRequest command (0x0002).</summary>
    public Task<InvokeResponse> GetDetailedForecastRequestAsync(
        CommodityPriceDetailBitmap details,
        CancellationToken ct = default)
        => InvokeCommandAsync(0x0002, tlv => { tlv.AddUInt16(0, (ushort)details); }, ct);

    // Attribute readers

    /// <summary>Read TariffUnit attribute (0x0000).</summary>
    public Task<TariffUnitEnum> ReadTariffUnitAsync(CancellationToken ct = default)
        => ReadAttributeAsync<TariffUnitEnum>(0x0000, ct);

    /// <summary>Read Currency attribute (0x0001).</summary>
    public Task<object?> ReadCurrencyAsync(CancellationToken ct = default)
        => ReadAttributeAsync(0x0001, ct);

    /// <summary>Read CurrentPrice attribute (0x0002).</summary>
    public Task<object?> ReadCurrentPriceAsync(CancellationToken ct = default)
        => ReadAttributeAsync(0x0002, ct);

    /// <summary>Read PriceForecast attribute (0x0003).</summary>
    public Task<CommodityPriceStruct[]?> ReadPriceForecastAsync(CancellationToken ct = default)
        => ReadRefAttributeAsync<CommodityPriceStruct[]>(0x0003, ct);

    // Event payload parsers

    private static PriceChangeEventData ReadPriceChangeEventData(MatterTLV tlv)
    {
        var value = new PriceChangeEventData();
        tlv.OpenStructure(7);
        while (!tlv.IsEndContainerNext())
        {
            switch (tlv.PeekTag())
            {
                case 0:
                    if (tlv.IsNextNull()) { tlv.GetNull(0); } else { value.CurrentPrice = ReadCommodityPriceStruct(tlv); }
                    break;
                default:
                    tlv.SkipElement();
                    break;
            }
        }

        tlv.CloseContainer();
        return value;
    }

    private static bool TryReadPriceChangeEventData(MatterEventReport report, out PriceChangeEventData? payload, out string? reason)
    {
        payload = null;
        if (report.RawData is null)
        {
            reason = "Event payload TLV was not captured.";
            return false;
        }

        try
        {
            payload = ReadPriceChangeEventData(new MatterTLV(report.RawData.GetBytes()));
            reason = null;
            return true;
        }
        catch (Exception ex)
        {
            reason = "PriceChange payload parse failed: " + ex.Message;
            return false;
        }
    }

    // Event payload JSON projectors

    private static JsonObject CreateCommodityPriceComponentStructJson(CommodityPriceComponentStruct value)
    {
        var json = new JsonObject();
        json["price"] = CreateJsonValue(value.Price);
        if (value.Source is { } source)
        {
            json["source"] = CreateJsonValue(source.ToString());
        }
        if (value.Description is { } description)
        {
            json["description"] = CreateJsonValue(description);
        }
        if (value.TariffComponentID is { } tariffComponentID)
        {
            json["tariffComponentID"] = CreateJsonValue(tariffComponentID);
        }
        return json;
    }

    private static JsonObject CreateCommodityPriceStructJson(CommodityPriceStruct value)
    {
        var json = new JsonObject();
        json["periodStart"] = CreateJsonValue(value.PeriodStart);
        if (value.PeriodEnd is { } periodEnd)
        {
            json["periodEnd"] = CreateJsonValue(periodEnd);
        }
        if (value.Price is { } price)
        {
            json["price"] = CreateJsonValue(price);
        }
        if (value.PriceLevel is { } priceLevel)
        {
            json["priceLevel"] = CreateJsonValue(priceLevel);
        }
        if (value.Description is { } description)
        {
            json["description"] = CreateJsonValue(description);
        }
        if (value.Components is { } componentsValues)
        {
            var componentsItems = new JsonArray();
            foreach (var item in componentsValues)
            {
                componentsItems.Add((JsonNode?)CreateCommodityPriceComponentStructJson(item));
            }
            json["components"] = componentsItems;
        }
        return json;
    }

    private static JsonObject CreateCurrencyStructJson(CurrencyStruct value)
    {
        var json = new JsonObject();
        json["currency"] = CreateJsonValue(value.Currency);
        json["decimalPoints"] = CreateJsonValue(value.DecimalPoints);
        return json;
    }

    private static JsonObject CreatePriceChangeEventDataJson(PriceChangeEventData value)
    {
        var json = new JsonObject();
        if (value.CurrentPrice is { } currentPrice)
        {
            json["currentPrice"] = CreateCommodityPriceStructJson(currentPrice);
        }
        return json;
    }

    internal static JsonObject? MapEventPayloadJson(ClusterEvent evt)
    {
        return evt switch
        {
            PriceChangeEvent typed => CreatePriceChangeEventDataJson(typed.Payload),
            _ => null,
        };
    }

    // Event readers and subscriptions

    /// <summary>Read event reports from this cluster.</summary>
    public async Task<ClusterEvent[]> ReadEventsAsync(
        uint[]? eventIds = null,
        bool fabricFiltered = false,
        CancellationToken ct = default)
    {
        var events = await ReadEventsAsync(MapEventReports, eventIds, fabricFiltered, ct);
        return [.. events];
    }

    /// <summary>Subscribe to event reports from this cluster.</summary>
    public Task<MatterEventSubscription<ClusterEvent>> SubscribeEventsAsync(
        uint[]? eventIds = null,
        ushort minInterval = 1,
        ushort maxInterval = 60,
        bool fabricFiltered = false,
        CancellationToken ct = default)
        => SubscribeEventsAsync(MapEventReports, eventIds, minInterval, maxInterval, fabricFiltered, ct);

    internal static ClusterEvent[] MapEventReports(IReadOnlyList<MatterEventReport> reports)
    {
        if (reports.Count == 0)
        {
            return [];
        }

        var events = new List<ClusterEvent>(reports.Count);
        foreach (var report in reports)
        {
            events.Add(MapEventReport(report));
        }

        return [.. events];
    }

    internal static ClusterEvent MapEventReport(MatterEventReport report)
    {
        return report.EventId switch
        {
            Events.PriceChange when TryReadPriceChangeEventData(report, out var priceChangeEventData, out _) => new PriceChangeEvent(report, priceChangeEventData!),
            Events.PriceChange when TryReadPriceChangeEventData(report, out _, out var priceChangeReason) => new UnknownClusterEvent(report, priceChangeReason),
            _ => new UnknownClusterEvent(report, "Event ID is not recognized by this cluster."),
        };
    }
}
